# 来源归档 · req-drag-lesson-editor

## 来源清单

| 编号 | 类型 | 来源 | 摘要 |
|------|------|------|------|
| S1 | 用户口述 | 2026-08-26 主会话 AskUserQuestion 答复 | "拖拽编辑不同的sim衔接，编辑剧本流程" —— 作者侧可视化拖拽编排 UI |
| S2 | 关联需求(done) | `requirements/req-lesson-runtime/spec/需求文档.md` v1.1 | lesson-plan 编排层完整实现：LessonPlan 数据模型、节点类型、条件树契约、入口卡片/进度/流转三件套；C-R5 明确"禁作者侧编排UI" |
| S3 | 关联需求(done) | `requirements/req-lesson-runtime/spec/最终需求.md` | 运行时已完成：58 AC + 5 条试点剧本 + manifest + schema + AI 生成链路 |
| S4 | 关联需求(done) | `requirements/req-lesson-runtime/notes.md` | 决策记录：回归守卫空洞（4条缺测）、手动冒烟未执行、rgb-default UNSAT 等遗留 |
| S5 | 关联需求(in_progress) | `requirements/req-lesson-scripting/notes.md` | 评估报告来源需求；#6 投影/命中检测技术债与 DragDropWorkspace 关联 |
| S6 | 项目约束 | `requirements/req-drag-lesson-editor/meta.yaml` design_constraints | 产物走 assets/lessons/*.json + lesson.schema.json；复用 L0 组件（PropertyControlPanel / DragDropWorkspace）；向后兼容 req-lesson-runtime 运行时 |
| S7 | 技术资产(已存在) | `schemas/lesson.schema.json` | 已有完整 JSON Schema：必填字段、advance.type enum、条件树叶子 type enum、routes 结构；scenarioRef.sim enum 限定 circuit/color_vision |
| S8 | 知识库 | `docs/knowledge/kratos/frontend/drag-drop-workspace.md` | DragDropWorkspace<T> 泛型组件：元件库拖入画布 + 投影坐标系 + 侧边/底部面板布局 |
| S9 | 用户答复(第1轮) | 2026-08-26 product-manager 追问 Q1 答复 | 承载形态 = App 内作者模式：复用 DragDropWorkspace 画布，不单独编译 |
| S10 | 用户答复(第1轮) | 2026-08-26 product-manager 追问 Q2 答复 | 编辑范围 = 仅编排现有结构：只创建/编辑符合 lesson.schema.json 的 JSON，不定义新原语 |
| S11 | 用户答复(第1轮) | 2026-08-26 product-manager 追问 Q4 答复 | 目标用户 = 两者兼顾：UI 友好（教师）+ 高级 JSON 模式（开发者） |
| S12 | 用户答复(第1轮) | 2026-08-26 product-manager 追问 Q5 答复 | AI 联动 = 导入已有 JSON（含 AI 生成）修改 + 从零新建 两条路径并存 |

## 原始素材原文

### S1 - 用户原始描述
> 拖拽式编排sim剧本需求
> 拖拽编辑不同的sim衔接，编辑剧本流程

### S6 - meta.yaml 已锁定约束
- 编辑产物必须走 assets/lessons/*.json + schemas/lesson.schema.json（AI 可生成化联动）
- 向后兼容 req-lesson-runtime 已实现的运行时（C3/C5 约束不破坏）
- 复用 L0 组件（PropertyControlPanel / DragDropWorkspace 等 · 禁平行实现）

### S9~S12 - 用户第 1 轮追问答复原文（2026-08-26）
- Q1 承载形态 = **A. App 内作者模式**：在 kratosLab Flutter App 内新增作者/编辑入口屏，复用 DragDropWorkspace 画布，不单独编译
- Q2 编辑范围 = **A. 仅编排现有结构**：只创建/编辑符合 lesson.schema.json 的 JSON（节点/条件路由/解锁），不定义新编排原语
- Q4 目标用户 = **C. 两者兼顾**：UI 友好（教师/课程设计者）+ 允许高级模式（开发者/技术运营可见 JSON 字段，可直接编辑 JSON）
- Q5 AI 联动 = **可以编辑现有的（例如 AI 生成），也可以从头开始**：编辑器支持导入已有 lesson JSON（含 AI 生成产物）进行修改，也支持从零新建——即"AI 生成→导入编辑→调整→保存"与"手动从头编排"两条路径并存
