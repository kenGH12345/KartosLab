# 修复 HomeScreen → ColorVisionHome 导航链某 Column overflow

> **状态**：存桩追踪（`status: pending`）· 未开工
> **来源**：`req-color-vision-layout-fix` Phase 3 integration test 首跑 (2026-08-06 16:52) 首次曝光
> **性质**：Bug 修复 · L0-2 违规
> **优先级**：待定（本存桩仅登记；实际开工时由 product-manager 排 P1/P2）

## 已知现象

`flutter test integration_test/color_vision_test.dart -d windows` 在 v1 版 fixture（走 HomeScreen → 点 "色觉·RGB合成" → 点 "Filter" tab 的完整导航链）下报：

```
A RenderFlex overflowed by 49 pixels on the bottom.
The overflowing RenderFlex has an orientation of Axis.vertical.

RenderFlex#d4b66 DISPOSED OVERFLOWING:
  parentData: offset=Offset(715.0, 0.0)
  constraints: BoxConstraints(0.0<=w<=1560.0, 0.0<=h<=139.0)
  size: Size(130.0, 139.0)
  direction: vertical
  mainAxisAlignment: center
  mainAxisSize: max
  crossAxisAlignment: center
```

- 测试视口：1600×900（`tester.view.physicalSize`）
- 触发时机：`tap('色觉·RGB合成') → settle → tap('Filter') → settle` 之间
- **已排除**：`lib/color_vision/screens/single_bulb_screen.dart`（本次 req-color-vision-layout-fix 已修复该屏，且 Column 尺寸 130×139 不符合主屏应有的填满视口的特征）
- **待定位**：溢出者极可能是 `lib/screens/home_screen.dart` 或 `lib/color_vision/screens/color_vision_home.dart` 或其 Magic Lab 首屏里某个 Row/Column 子容器

## 违反的规则

- `.codebuddy/rules/80-kartosos-sim-checklist.mdc` §七 L0-2 "响应式无溢出"（1600×900 视口在项目支持的 320-1920×480-1080 区间内）

## 未开工前需要做的

（留给未来 phase 1.intake → 2.requirement 时的 product-manager / 主会话补齐）

1. 定位溢出 Column 的真实 creator（需要 flutter run -d windows 手动复现 + widget inspector）
2. 判定是否 HomeScreen 主 Column（7 个 FilledButton 自然堆高）还是 ColorVisionHome 里的 Row 子布局
3. 320px / 1024px / 1920px 三视口审计现象覆盖面
4. 决定修法：Wrap ScrollView / 折叠 / LayoutBuilder + FittedBox / 其他

## 不做（本存桩不越权）

- 不定 AC（`80-checklist §一` 允许 stub 阶段暂缺；实际开工时补）
- 不动 [home_screen.dart](c:\workspace\kartososLab\lib\screens\home_screen.dart) 一行代码
- 不派 agent

## 关联需求

- **上游触发**：`../req-color-vision-layout-fix/process.txt:10` （2026-08-06 16:52 事件）
- **上游 test 文件溯源注释**：`integration_test/color_vision_test.dart:23-25`
