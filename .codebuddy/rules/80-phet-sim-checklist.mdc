---
alwaysApply: true
---
# Phet Sim 开工自检表（New Sim Onboarding Checklist）

> **位置**：`.codebuddy/rules/80-phet-sim-checklist.mdc`
> **适用范围**：任何新的 phet-java-simulations→Flutter 复刻需求开工时（phase 1.intake → 2.requirement 切换前），主会话或 product-manager **必须逐条通过本表**
> **创建原因**：2026-07-24 审计发现 context 文档（overview / shared-abstraction-plan / edd-template）定义了"应该做什么"，但 10 个 rules 文件中无任何规则要求 agent 去查。color-vision EDD 遗漏可配置化/AI 可生成化/通用化组件清单为直接实证。
> **与其他规则的关系**：本规则是 `20-verify-before-act.mdc` 知识库优先顺序的**下游执行清单**——它告诉你"查到 context 后要检查什么"
> **强制程度**：**阻塞级**。未通过自检表的 sim，不允许从 phase 1 切换到 phase 2。

---

## 一、开工前必读清单（Reading Order · 阻断-漏-根因）

本 sim 开工前，按顺序读以下文件：

| # | 文件 | 读后确认什么 |
|---:|---|---|
| 1 | `docs/knowledge/phet-java-simulations/overview.md` | 确认理解项目四原则（MVC / 组件化 / 通用化 / 配置化） |
| 2 | `docs/knowledge/phet-java-simulations/shared-abstraction-plan.md` | 确认理解 L0/L1/L2 三层组件体系 + §七 3 条门禁 |
| 3 | `docs/knowledge/phet-java-simulations/edd-template.md` | 确认理解 12 章 EDD 结构（含 §9-§12） |
| 4 | `requirements/<req-id>/meta.yaml` | 确认 `edd_template_version` 字段 = `v2.0` |
| 5 | `.codebuddy/rules/80-phet-sim-checklist.mdc`（本文件） | 逐条通过 §二-§五 + §七 自检表 |

**违反后果**：跳过步骤 1-3 的 sim，在 phase 2 requirement 产出的 EDD **必然遗漏**至少一个必答项（已验证实证：color-vision EDD v1 遗漏可配置化/AI 可生成化两整章）

---

## 二、项目四原则逐条自查

每开工一个新 sim，必须在 spec 阶段（`product-manager` 出需求文档时）逐条回答：

### 原则 1 · MVC 架构

- [ ] 本 sim 的 Model / View / Controller 三层分别在哪？（对应 `lib/<sim>/model/` `view/` `controller/` 目录）
- [ ] 是否存在 View 直接改 Model 字段的耦合？（如果是 → 违规 · 需修）
- [ ] Controller 的职责边界是否明确？（tick loop 谁做 · notifyListeners 谁调 · model 谁改）

### 原则 2 · 元件化 / 组件化（EGPSpace）

- [ ] 本 sim 有哪些物理元件？（对齐 EDD §2.1）
- [ ] 每个物理元件是否有独立的 CustomPainter？（一元件一 Painter · 禁止一个 God Painter 画全部）
- [ ] Model 层和 Render 层是否通过接口通信？（Model 不依赖 Flutter · Painter 不改 x/y）

### 原则 3 · 通用化（L0/L1/L2 · 3-Time Rule）

**见 §三 专项自检**

### 原则 4 · 配置化（JSON Scenario + AI 可生成化）

- [ ] 本 sim 的 Intrinsic 参数是否全部写入 scenario JSON？（对齐 EDD §3.1）
- [ ] `schemas/<sim>_scenario.schema.json` 是否已创建？
- [ ] `docs/prompts/<sim>_scenario.md` 是否已创建？（AI 可生成化 prompt）
- [ ] UI 右侧参数面板是否通过 `PropertyControlPanel.fromScenarioParams` 生成？
- [ ] 加载失败的场景是否降级为 default 场景（不 crash）？

---

## 三、通用组件复用自检（G1/G2/G3 · 阻断-违规-造轮）

> **执法依据**：`shared-abstraction-plan.md` L0/L1/L2 三层体系 + §七 门禁
> **本段 3 条自检项 = 该 plan 第 5-7 条门禁在规则层的显式落地**

### G1 · L0 复用强制

**如果本 sim 需要以下任意控件，必须先 grep `lib/common/` 确认已有实现，直接用，禁止模拟内平行实现：**

| 控件 | L0 路径 | 如未用需说明 |
|---|---|---|
| Slider | `lib/common/controls/phet_slider.dart` | □ |
| ComboBox | `lib/common/controls/phet_combo_box.dart` | □ |
| RadioGroup | `lib/common/controls/phet_radio_group.dart` | □ |
| NumberField | `lib/common/controls/phet_number_field.dart` | □ |
| PropertyPanel | `lib/common/widgets/property_control_panel.dart` | □ |
| TimeControlBar | `lib/common/widgets/time_control_bar.dart` | □ |
| SimulationClock | `lib/common/simulation_clock.dart` | □ |
| Chart | `lib/common/chart/` | □ |
| ScenarioManagerBase | `lib/common/scenario/scenario_manager_base.dart`（P2 完工后） | □ |

- [ ] **所有必用 L0 组件已确认复用（或已说明不用的理由）**

**违规后果**：如果在 sim 本地造了 L0 已存在的组件平行实现 → 视为违规 · code-reviewer 阶段退回

### G2 · 新增组件审批

若需要 L0 不存在的控件：

- [ ] 已确认"这不是已有 L0 组件的变体"（grep 过 `lib/common/`）
- [ ] 新组件已登记到 `shared-abstraction-plan.md` L1 候选表（含文件名 + 使用者计数）
- [ ] 已判断是否触发 3-Time Rule（见 G3）

### G3 · 3-Time Rule（组件版）

同 `10-vibecoding-protocol.mdc` §5 的 Skill 版**语义不同**，组件版触发条件为：

| 使用者数 | 规则 |
|---|---|
| **第 1 个 sim** | 在本 sim 内造 · 登记到 `shared-abstraction-plan.md` L1 · 标记"第 1/3 用户" |
| **第 2 个 sim** | 比较与第 1 个的相似度 · ≥ 70% 则改造第 1 个为 `lib/common/` 公共版 |
| **第 3 个 sim** | **强制上抽**到 `lib/common/` · 不允许第 3 个还在 sim 内重复造 |

- [ ] 本 sim 是否触发了某 L1 候选的第 2 或第 3 用户门槛？
- [ ] 若触发第 3 用户 → 是否已执行上抽？（未执行 = 阻塞）
- [ ] 本 sim 是否引入了"疑似 L1"候选？（若是 → 登记在 `notes.md`）

---

## 四、EDD 模板版本检查

- [ ] `meta.yaml` 中 `edd_template_version` 字段 = `v2.0`
- [ ] 产物 EDD 覆盖 12 章全量（grep 验证：`grep -c "^## §[0-9]" edd/<sim>-EDD.md` 应输出 ≥ 10）
- [ ] 所有 `- [ ]` 必答项已勾选为 `- [x]`（grep 验证：`grep -c "\[ \]" edd/<sim>-EDD.md` 应输出 0）

**版本字段的审计价值**：当 audit 脚本或 tech-leader 审查时，`edd_template_version: v2.0` 表明该需求遵循了含 §9-§12 的完整模板；缺失此字段或版本号 < v2.0 则需人工复查是否遗漏配置化/AI 可生成化/通用化/质量属性章节。

---

## 五、学科正确性与教学有效性附加检查

> 以下不属于"项目四原则"漏项，但从 color-vision 经验看同样容易遗漏

- [ ] EDD §6 现象声明（Q8）是否包含"无现象声明段"（如无声/无温度/无力学）？
- [ ] EDD §7 学生交互（Q9）是否包含"无操作场景段"（哪些看起来该能操作但不行的）？
- [ ] EDD §5 教学脚本是否包含"反直觉现象清单"≥ 2 条？
- [ ] EDD §12 质量属性声明中是否包含"可访问性声明"（色盲替代表征等 · 视觉实验特需）？

---

## 六、违反后果与执行时机

| 违反项 | 后果 | 谁执行 |
|---|---|---|
| 未通过 §一 开工必读 | product-manager 不允许进入 phase 2 | tech-leader / 主会话 |
| 未通过 §二 四原则自查 | spec 退回 phase 1 · 补全后重新提交 | code-reviewer |
| 未通过 G1（L0 复用） | 在 sim 内平行实现 L0 组件 → code-review 阶段退回 | code-reviewer |
| 未通过 G2（新增组件未审批） | 新组件未登记到 shared-abstraction-plan.md → 退回补登记 | tech-leader |
| 未通过 G3（第 3 用户未上抽） | 阻塞 · 必须先执行上抽再继续本 sim | tech-leader |
| 未通过 §四 EDD 版本 | spec 退回 · 补 `edd_template_version: v2.0` 后重新提交 | product-manager / 主会话 |
| 未通过 §七 L0-1（主图未居中） | UI 改动退回 · 补居中包裹后重新提交 | code-reviewer |
| 未通过 §七 L0-2（有溢出） | UI 改动阻塞 · 必修 | code-reviewer |
| 未通过 §七 L0-3（硬编码尺寸） | UI 改动退回 · 换为 LayoutBuilder | code-reviewer |
| 未提交 §七 M1 三视口截图 | commit 阻塞 | code-reviewer |

**执行时机**：主会话在 phase 1.intake → 2.requirement 阶段切换时，必须逐条通过本表并在 `process.txt` 追加一行：

```
[time] 80-phet-sim-checklist: 全部通过（G1=已复用 N 个 L0 组件 · G2=无新增 · G3=未触发第3用户 · EDD=v2.0 · layout=居中✓+无溢出✓+响应式✓）
```

---

## 七、屏幕适配与画面居中（Layout Standards）

> **执法依据**：项目硬性要求，2026-07-28 用户决策 `A · G1 · Q · M1`。
> **强制程度**：**阻塞级** L0 三条，与 §二-§五 同权重。
> **性质**：跨 sim 通用工程约束 · 与项目四原则并列的第 5 条工程铁律。

### L0 · 必须条款（阻塞级 · 三条）

所有 phet sim 的主屏幕（`lib/<sim>/screens/<sim>_screen.dart` 或等价入口）必须同时满足：

#### L0-1 · 主图/舞台水平居中

- [ ] 主图容器外层使用 `Center` / `Align(alignment: Alignment.center)` / `Row(mainAxisAlignment: MainAxisAlignment.center)` 之一包裹
- [ ] **禁止**用 `Positioned(left: <常数>)` 绝对定位主图元素
- [ ] **禁止**用 `Padding(left: <大常数>)` 模拟居中（会随视口宽度失衡）
- [ ] 若主图内部还有子舞台（如 sound sim 的 Tab 切换视图），每个 Tab 的内容也必须遵守本条

**自检 grep**：
```
grep -rn "Positioned(left:\|Padding(padding: const EdgeInsets.only(left:" lib/<sim>/screens/
```
输出应为空，或每一处都有行内注释说明为什么必须绝对定位。

#### L0-2 · 响应式适配 · 无溢出

- [ ] `flutter analyze` 无 layout 类 warning
- [ ] 运行时无 `A RenderFlex overflowed by X pixels` 报错
- [ ] 视口宽度 320px（手机竖屏最窄）至 1920px（桌面）之间均无横向滚动条
- [ ] 视口高度 480px 至 1080px 之间主要控件均可见（不需要滚屏才能找到关键交互）

**自检做法**：本地 `flutter run -d windows` 后手动拖窗口最小宽度，观察是否有黄黑警戒条纹或红色 error banner。

#### L0-3 · 主图尺寸随视口缩放

- [ ] 主图（Canvas / CustomPaint / 主 Widget）使用 `LayoutBuilder` 或 `MediaQuery.of(context).size` 计算尺寸
- [ ] 至少满足自适应下限：`side = min(viewportW × 0.6, viewportH × 0.7)` 或等价约束
- [ ] **禁止**硬编码 `SizedBox(width: 800, height: 600)` 之类固定像素
- [ ] 允许硬编码的是：控件面板宽度（如右侧参数条 240px）、字体级别基线、图标尺寸；不允许硬编码的是：主渲染区、主 Canvas、Tab 内容区

### M1 · 截图证据要求

UI 类改动 commit 前必须提供 **3 种视口截图** 作为可视反馈证据（对齐 `10-vibecoding-protocol.mdc` 第 3 条可视反馈优先）：

| 视口尺寸 | 场景 | 文件名约定 |
|---|---|---|
| 375×667 | 手机竖屏 | `<sim>-mobile-portrait.png` |
| 1024×768 | 平板横屏 | `<sim>-tablet-landscape.png` |
| 1920×1080 | 桌面 | `<sim>-desktop.png` |

- [ ] 3 张截图存于 `requirements/<req-id>/screenshots/`
- [ ] `process.txt` 追加一行：`[time] layout-screenshots: 3 视口已提交（mobile/tablet/desktop）`
- [ ] `code-reviewer` 在 review 时验证 3 张截图存在，缺少 → 阻塞合入

### 存量 sim 一次性审计（Q 决策）

本规则生效后 7 天内，主会话应为以下 4 个已完成 sim 各出一份专项审计报告（存量 sim 不强制立整改，但需标记历史债务）：

- sound
- radio-waves
- wave-interference
- color-vision

审计产物位于 `docs/knowledge/phet-java-simulations/layout-audit-2026-07.md`，含每 sim 当前 L0-1 / L0-2 / L0-3 合规性 + 病灯标记（🟢/🟡/🔴）。

---

## 八、向 v3.0（C 演进）迁移

当以下条件全部满足时，触发 C 演进：

- [ ] 4 个 sim 的完整 EDD（含 §9-§12）全部产出并验证
- [ ] ≥ 2 次"主会话遗漏自检项"的实证记录在 process.txt
- [ ] 本自检表的 G1/G2/G3 已被 ≥ 2 个 sim 实际使用并验证实用

届时操作（走 `40-agent-self-evolution.mdc` 5 步协议）：

1. 改 `product-manager` agent prompt · 在启动步骤追加"必读 `80-phet-sim-checklist.mdc`"
2. 改 `tech-leader` agent prompt · 在审查步骤追加"逐条验证 G1/G2/G3"
3. 本文件 version 字段升级为 `v3.0` · agent 消费方式从"人工纪律"升级为"agent 强制"

---

## 变更历史

> 2026-07-28 by 主对话 · 用户决策 A · G1 · Q · M1：
> 新增 §七 屏幕适配与画面居中（Layout Standards）· 项目级硬性布局规则落地。
> - 承载 3 条 L0 阻塞条款：L0-1 主图水平居中 / L0-2 响应式无溢出 / L0-3 主图尺寸随视口缩放
> - 承载 M1 截图证据要求（3 视口截图 · 存 `requirements/<req-id>/screenshots/`）
> - 承载 Q 决策：存量 sim 仅一次性审计不强制整改 · 未来 sim 强制执行
> - 触发原因：sound sim 图 1/2 视觉稿评审用户明确要求

> 2026-07-24 by 主对话（用户决策 B + 逐步向 C 演进 · 统一落地所有待审计缺口）：
> 规则初版。闭合 context 文档与 rules 文件之间的桥接缺口。
> - 承载 D1（开工自检表）+ D2（L0 复用强制）+ D3（新增组件审批）+ D4（组件级 3-Time Rule）
> - 触发原因：color-vision EDD 先后遗漏可配置化/AI 可生成化/通用化组件清单 · 审计发现根因是 context 定义"该做什么"但无规则要求 agent 去查
> - 执行时机：phase 1→2 切换时 · 主会话逐条通过并在 process.txt 留底
> - v2.0→v3.0 路标：4 sim EDD 全产出 + ≥ 2 次"漏项"实证后走 40-agent-self-evolution 协议内嵌到 agent prompt