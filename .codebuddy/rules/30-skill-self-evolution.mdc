---
description: Skill 自进化协议——发现 Skill 过时不允许静默改，必须按 5 步先提案再修改。
alwaysApply: true
---

# Skill 自进化协议

Skill 是协作的标准操作手册。如果 AI 可以"顺手改一下"，几个 session 后就没人知道为什么 Skill 变成现在这样、变更基于哪次实践——整套体系就废了。

## 硬约束

任何 agent / 主会话 / 命令都**严禁**：

- 直接 Edit / Write `.codebuddy/skills/**/SKILL.md`
- 直接 Edit / Write `.codebuddy/skills/**/references/*.md`
- "顺便修一下错别字"也不行——错别字也要走流程

唯一允许的修改方式：**5 步协议**。

## 5 步协议

### 1. 触发判定

发现以下任一情况，触发协议：

- Skill 描述与实际行为不符
- Skill 引用的工具/路径已不存在
- Skill 步骤里有明显疏漏（按它做完拿不到声明的产物）
- 同一类问题被同一 Skill 处理失败 ≥ 2 次
- 用户明确指出 Skill 有问题

不触发的情况：

- 你"觉得可以更好"——这不是触发条件
- 单次特殊场景需要——单次特殊处理就够了，不要改 Skill
- 没有 3 次以上的重复证据——遵循 3-Time Rule

### 2. 诊断

写明：

- 现状：当前 Skill 是怎么写的（引用具体行号）
- 现象：实际跑出来什么问题（要有真实样本）
- 根因：为什么会这样（不要停在"AI 没按 Skill 做"，要查 Skill 本身）

### 3. 拟定修改

写明：

- diff：要改哪几行（before / after）
- 影响面：还有哪些 Skill / agent / 命令引用了这部分？要不要联动？
- 兼容性：会不会让已经用旧 Skill 跑出来的产物变得不可读？

### 4. 输出提案 + 等待用户回复

把上面两段（诊断 + 拟定）发给用户，**等用户回 y/n**。

不允许：

- 不等用户回复直接改
- 用户没回复就当作"默认同意"
- 用户说"看着办"就跳到第 5 步——必须显式 y/n

### 5. 应用 + 记录变更历史

用户回 `y` 后：

- 应用改动
- 在 Skill 文件末尾追加一行变更历史：
  ```
  > 2026-04-29 by code-reviewer: 修第 47 行的工具名（原 git-format → git fmt）。理由：F-1.x，详见 .codebuddy/docs/PHASE5-FINDINGS.md
  ```
- 在主会话汇报"已按用户确认应用变更"

用户回 `n`：**文件不动**，把诊断写到当前需求的 `notes.md` 备查。

## 与其他规则的关系

- "不可逆操作需用户确认" → `20-verify-before-act.mdc`
- 触发时机 "做了 3 次" → `10-vibecoding-protocol.mdc` 第 5 条
- 详细操作手册 → `.codebuddy/skills/_meta/self-evolution-protocol.md`
- SOP 自进化（同构对照）→ `35-sop-self-evolution.mdc`
