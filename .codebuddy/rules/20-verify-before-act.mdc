---
description: 行动前必须验证——95% 信心再动手 + 任何技术判断前先读项目知识库。这是 agent 失控的最大防线。
alwaysApply: true
---

# 行动前验证

这条规则把两件事合并成一个原则：**信心不足时不要动手**。

它统合了：

- **追问用户**（曾用名 `ask-before-act`）：信心 < 95% 时问而不是猜
- **先读知识库**（曾用名 `knowledge-base-first`）：技术判断前先读 `context/`

它们是同一件事的两面：**动手前先把"我可能猜错"的概率降下来**。

## 信心阈值

| 你对将要做的事的把握 | 该怎么办 |
|---|---|
| ≥ 95% | 直接做，做完留底（`process.txt`） |
| 70-95% | 做，但**先告知**用户你的理解 + 你打算做什么；做完汇报 |
| < 70% | **停**，问用户或读资料，把把握升到 ≥ 70% 再继续 |

不允许"边做边问"——边做意味着已经在某种假设上动手了。

## 追问用户的规则

需要追问时：

- **一次性问完**（用 `AskQuestion` 工具一批 ≤ 5 个问题）
- 提供**结构化选项**而不是开放式问题
- 给出每个选项的权衡分析
- 如果有"推荐选项"明确指出来

### 追问选项设计规范（与 `.claude/agents/product-manager.md` 四层体系对齐）

每个问题的选项必须覆盖 **What → Why → How → What-if** 中对应层的完整可能性：

| 选项 | 含义 | 适用层 | 权衡 |
|------|------|--------|------|
| **A. 确认（推荐）** | 当前理解正确 | What | 最简路径，但可能漏隐含约束 |
| **B. 偏差修正** | 当前理解有偏差，需调整 | What | 获得正确信息，但增加一轮交互 |
| **C. 补充信息** | 当前不完整，需补充 | What/Why | 提高完整性，但增加响应量 |
| **D. 不知道** | 用户也还没想清楚 | Why/What-if | 诚实标注[待确认]，避免猜测 |
| **E. 接受建议** | 让 AI 基于上下文推荐 | How/What-if | 用户减负，但需 AI 承担决策风险 |

**各层选项设计原则**：

- **What 层**：选项应为"是/否/部分/不适用"的穷尽集合，消除需求两义性
- **Why 层**：选项应覆盖"业务目标/合规要求/用户体验/技术依赖/其他"等动机分类
- **How 层**：选项应包含"有固定约束/有偏好但不强制/无偏好/接受建议"四种状态
- **What-if 层**：每个选项应附带预期行为 + 回退策略，或显式标注"未考虑此场景"

> 结构化选项的核心价值：把用户从"创作答案"变成"做选择"，降低认知负荷、提高响应速度。
> 若 5 个选项无法穷尽可能性，最后一项固定为"其他（请补充）"。

不允许：

- 一次只问一个问题、来回好几轮
- 让用户做你应该做的研究（如 "你想用什么数据库？"——你应该先看 `context/project/<name>/` 有没有约定）
- 把"为了显得专业而问"和"真的需要确认"混淆

## 知识库优先（查找顺序 + 自证引用）

任何 agent 在做技术判断前**必须**按这个顺序查：

1. **当前需求**：`requirements/<id>/spec/`、`design/`、`notes.md`
2. **项目知识库**：`docs/knowledge/phet-java-simulations/INDEX.md` 及 `docs/knowledge/phet/INDEX.md` 及其引用的具体文档
3. **源码搜索**：以上都没有时再 grep `lib/`

### 自证引用（硬约束）

**每当你基于第 2 层（项目知识库）信息做出技术判断时，必须在回复中显式引用具体文档路径**，格式为：

```
[来源: docs/knowledge/phet/architecture/design-patterns.md:45-67]
```

- 如果回复中**没有任何 `[来源: docs/knowledge/...]` 格式的引用**，视为违规——说明你跳过了知识库，直接凭直觉或源码猜测做出判断
- 引用必须精确到文件路径 + 行号范围（如 `overview.md:12-15`），不允许只写文件名
- 本约束对 agent 和主会话均生效

不允许：

- 跳过知识库直接 grep 源码后下结论
- 凭直觉说"应该用 X"
- 编造一个 `docs/knowledge/` 中根本没有的"项目约定"
- 回复中做了技术判断但没有 `[来源: docs/knowledge/...]` 引用痕迹

## 例外：何时绕过 95% 阈值

以下情况可以**直接做**：

- 纯机械操作（重命名变量、移动文件位置）
- 用户明确说 "你看着办" / "随便选"

以下情况**反过来需要 100% 信心 + 用户明确确认**：

- 不可逆操作（`svn commit`、`svn delete`、`rm -rf`、DB migration）
- 改 SOP / Skill 等"协作标准"文件
- 删除任何文件（哪怕看起来没用）

## 与其他规则的关系

- 信心不足时的 stop 行为 → `10-vibecoding-protocol.mdc` 第 4 节
- 知识库的"单一源"原则 → `45-state-sync-protocol.mdc`
- Skill 的修改不允许静默 → `30-skill-self-evolution.mdc`
