---
description: Vibecoding 5 条核心约束——30 分钟原则、小步快跑、可视反馈、状态先写、3-Time Rule。整个 SOP 体系的工程基础。
alwaysApply: true
---

# Vibecoding 协议

Vibecoding 是本模板的**工程哲学**。SOP（agile-vibe / deep-vibe）是流程实现，但流程之上是这 5 条原则。

## 5 条核心

### 1. 30 分钟原则

任何 vibe-loop 单次循环的目标，必须能在 **30 分钟内拿到可见反馈**：

- 不能定 "重构整个数据层" 这种 1+ 小时的目标
- 拆到 "把 UserService 的 getById 改成异步并跑通单测" 这种 30 分钟级别
- 30 分钟到了仍没反馈就 stop，倒推是否目标定大了

### 2. 小步快跑

- 单次 commit 对应**单一改动**
- 不要 "加完功能 + 修了顺手发现的 3 个 bug + 重命名了 2 个变量" 一起提交
- 每个 commit 都应该可以独立 review、独立回滚

### 3. 可视反馈优先

改完代码必须**亲眼看到反馈**：

- UI 改动 → 跑 dev server / 截图 / 端到端预览
- 后端改动 → 跑单元测试 / 集成测试 / 看日志
- 工具脚本 → 真的跑一遍

不允许"看测试代码觉得应该过"就算完成。也不允许"AI 说改好了"就 commit。

### 4. 状态先写，后做

任何外部可观察的状态变化前，**先**写到 `meta.yaml` + `process.txt`，**再**做实际动作。

详见 `45-state-sync-protocol.mdc`。

### 5. 3-Time Rule（痛点即 Skill）

同一类操作做到第 3 次时，触发 `/skill-new` 把它封装：

- 第 1 次：手工做
- 第 2 次：仍可以手工
- 第 3 次：必须考虑沉淀为 Skill 或加入 `context/`

不允许"第 4 次还在重复手工的事"。

## 何时 stop

vibe-loop 出现以下任一信号，**立即停**，不要继续猛冲：

- 30 分钟到了仍没拿到反馈
- 同一个错误连续 3 次没修好
- 信心 < 70% 但仍在写代码
- 在改"这次需求范围之外"的东西

stop 之后到 `process.txt` 写一行说明，然后退回主会话评估。

## 与其他规则的关系

- 第 4 条 → `45-state-sync-protocol.mdc`
- 第 5 条 → `30-skill-self-evolution.mdc`
- 信心阈值 → `20-verify-before-act.mdc`
