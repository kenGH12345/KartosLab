---
description: 渐进式输出——先给摘要再展开细节、用结构化选项让用户做决定、避免一上来就堆几屏文字。
alwaysApply: true
---

# 渐进式输出

AI 一次回复堆几屏文字是常见反模式。它让用户：

- 来不及读完就要做决定
- 看不出哪些是关键、哪些是补充
- 难以挑出选项做选择

这条规则规范"怎么把信息逐层呈现给用户"。

## 三层结构

每次回复（特别是给用户做决策时）按这个层次：

### 第 1 层：摘要 / 结论（必须）

- 1-3 句话说明"做了什么 / 找到什么 / 推荐什么"
- 用户读到这里就该能知道下一步该问什么
- 不要把摘要藏在细节中间

### 第 2 层：关键证据 / 选项（必须）

- 如果是技术结论：列引用回源（`<file>:<line>` 或 URL）
- 如果是决策点：用结构化选项（表格 / `AskQuestion` 工具）
- 不要让用户在长段落里挖关键信息

### 第 3 层：可展开的细节（可选）

- 用户问"为什么"时再展开
- 或用"详见 X 文件"指向位置，让用户自己决定是否深入
- 不要默认全部摊开

## 决策点的标准格式

需要用户拍板时：

| 选项 | 利 | 弊 | 推荐？ |
|---|---|---|---|
| A | ... | ... | ✅（标明为什么） |
| B | ... | ... | |
| C | ... | ... | |

如果有 `AskQuestion` 工具可用，直接用工具问，不要让用户手敲选项编号。

## 报告改动结果时

```
## 变更摘要
- 改了 X / Y / Z 文件
- 修了 N 个问题

## 关键改动
- <file>:<line> —— 简述

## 验证结果
- 跑了 X 测试：通过
- 跑了 Y 校验：报 N 项警告

## 你的下一步
- 如果同意，继续 ...
- 如果想看具体 diff，告诉我看哪个文件
```

不允许：

- 大段散文式叙述
- "我做了 A，然后做了 B，然后做了 C..." 流水账
- 把改动结果埋在解释中间

## 长任务的进度报告

执行多步任务时，每完成一个里程碑给一个**简短**的进度更新：

```
✓ 步骤 1 完成
→ 步骤 2 进行中
□ 步骤 3-5 待办
```

不要等全做完才一次性汇报。也不要每一小步都汇报刷屏。

## 何时可以输出长内容

- 用户**明确问**"详细解释一下" / "完整列出"
- 写规范文档 / 长决策记录
- 输出代码（代码本身需要的篇幅就给）
- 用户在等"复盘 / 总结" 类任务

其他情况都倾向于**精简 + 可展开**。

## 与其他规则的关系

- 引用先行 → `60-citation-and-honesty.mdc`
- 用结构化选项问用户 → `20-verify-before-act.mdc`
- 状态摘要先于细节 → 同 `45-state-sync-protocol.mdc` 的 process.txt 写法
